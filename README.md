# mongodb-goplayground
Super simple MongoDB example (access, insert, find) in Go for personal learning

# Usage

```sh
$ mongod
$ mongo
> use employeeDB
> some docment command (insertOne, findOne, find, ....)
```

After that please run 
```sh
go run main.go
```
